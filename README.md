# Регистр Алектиниб

Веб-приложение для регистра клинических случаев лечения алектинибом при немелкоклеточном раке легких с ALK-позитивным статусом.

## 📋 Содержание

- [Описание](#описание)
- [Технологии](#технологии)
- [Структура проекта](#структура-проекта)
- [Быстрый старт (разработка)](#быстрый-старт-разработка)
- [Деплой на VPS](#деплой-на-vps)
- [Первый вход](#первый-вход)
- [Обслуживание](#обслуживание)

## 🎯 Описание

Приложение предназначено для ведения реестра пациентов, получающих лечение алектинибом. Включает:

- **Аутентификация** с ролями (Администратор / Пользователь)
- **CRUD операции** для пациентов и клинических записей
- **Управление справочниками** (только для администратора)
- **Аналитическая справка** по учреждениям
- **Audit log** всех операций
- **Soft delete** (is_active вместо физического удаления)

### Возможности

#### Для всех пользователей:
- Просмотр и добавление пациентов своего учреждения
- Редактирование клинических записей
- Полная форма с валидациями и условной логикой полей

#### Для администраторов:
- Доступ ко всем данным всех учреждений
- Управление справочниками через веб-интерфейс
- Просмотр аналитики (количество записей, % заполнения полей)
- Управление пользователями и учреждениями

## 🛠 Технологии

### Backend:
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - база данных
- **Pydantic** - валидация данных
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей

### Frontend:
- **React 18** - библиотека UI
- **Vite** - сборщик
- **React Router** - маршрутизация
- **Axios** - HTTP клиент

## 📁 Структура проекта

```
alectinib_registry/
├── backend/                    # Backend приложение
│   ├── main.py                # Основное приложение FastAPI
│   ├── models.py              # Модели SQLAlchemy
│   ├── schemas.py             # Pydantic схемы
│   ├── database.py            # Настройка БД
│   ├── auth.py                # Аутентификация и авторизация
│   ├── init_db.py             # Скрипт инициализации БД
│   ├── requirements.txt       # Python зависимости
│   └── .env.example           # Пример конфигурации
├── frontend/                   # Frontend приложение
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   ├── pages/             # Страницы приложения
│   │   ├── services/          # API сервисы
│   │   ├── styles/            # CSS стили
│   │   ├── App.jsx            # Корневой компонент
│   │   └── main.jsx           # Точка входа
│   ├── package.json           # Node.js зависимости
│   ├── vite.config.js         # Конфигурация Vite
│   └── index.html             # HTML шаблон
├── config/                     # Конфигурационные файлы
│   ├── alectinib-registry.service  # Systemd service
│   └── alectinib-registry.conf     # Nginx конфигурация
├── start_backend.sh           # Скрипт запуска backend (dev)
├── start_frontend.sh          # Скрипт запуска frontend (dev)
└── README.md                  # Документация
```

## 🚀 Быстрый старт (разработка)

### Предварительные требования

- Python 3.9+
- Node.js 18+
- npm или yarn

### 1. Клонирование репозитория

```bash
cd /home/ubuntu/alectinib_registry
```

### 2. Настройка Backend

```bash
# Переход в директорию backend
cd backend

# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Инициализация базы данных
python init_db.py

# Запуск сервера разработки
python -m uvicorn main:app --reload --host 0.0.0.0 --port 5000
```

Backend будет доступен по адресу: http://localhost:5000

API документация (Swagger): http://localhost:5000/docs

### 3. Настройка Frontend

Откройте новый терминал:

```bash
# Переход в директорию frontend
cd frontend

# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Frontend будет доступен по адресу: http://localhost:3000

### Альтернативный запуск через скрипты

```bash
# Backend
chmod +x start_backend.sh
./start_backend.sh

# Frontend (в отдельном терминале)
chmod +x start_frontend.sh
./start_frontend.sh
```

## 🌐 Деплой на VPS

### Требования к серверу

- Ubuntu 20.04+ / Debian 11+
- Python 3.9+
- Node.js 18+
- Nginx
- 1 GB RAM минимум
- 10 GB дискового пространства

### Шаг 1: Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка необходимых пакетов
sudo apt install -y python3 python3-pip python3-venv nginx nodejs npm git
```

### Шаг 2: Копирование проекта на сервер

```bash
# Создание директории для проекта
sudo mkdir -p /var/www/alectinib_registry
sudo chown -R $USER:$USER /var/www/alectinib_registry

# Копирование файлов (с локальной машины)
scp -r /home/ubuntu/alectinib_registry/* user@your-server:/var/www/alectinib_registry/

# Или клонирование из git (если настроен)
# cd /var/www/alectinib_registry
# git clone your-repo-url .
```

### Шаг 3: Настройка Backend

```bash
cd /var/www/alectinib_registry/backend

# Создание виртуального окружения
python3 -m venv ../venv

# Активация окружения
source ../venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Создание директории для БД
sudo mkdir -p /alectinib_registry/data
sudo chown -R www-data:www-data /alectinib_registry/data

# Настройка переменных окружения
cp .env.example .env
nano .env  # Измените SECRET_KEY на случайную строку!

# Инициализация базы данных
python init_db.py
```

**⚠️ ВАЖНО: Измените SECRET_KEY в .env файле!**

Сгенерируйте случайный ключ:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### Шаг 4: Сборка Frontend

```bash
cd /var/www/alectinib_registry/frontend

# Установка зависимостей
npm install

# Сборка для production
npm run build

# После сборки статические файлы будут в frontend/dist/
```

### Шаг 5: Настройка Systemd Service

```bash
# Копирование service файла
sudo cp /var/www/alectinib_registry/config/alectinib-registry.service /etc/systemd/system/

# Редактирование файла (если нужно изменить пути или настройки)
sudo nano /etc/systemd/system/alectinib-registry.service

# ВАЖНО: Обновите SECRET_KEY в Environment переменной!

# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска
sudo systemctl enable alectinib-registry

# Запуск сервиса
sudo systemctl start alectinib-registry

# Проверка статуса
sudo systemctl status alectinib-registry
```

### Шаг 6: Настройка Nginx

```bash
# Копирование конфигурации
sudo cp /var/www/alectinib_registry/config/alectinib-registry.conf /etc/nginx/sites-available/

# Редактирование конфигурации
sudo nano /etc/nginx/sites-available/alectinib-registry.conf

# Замените "your-domain.com" на ваш домен или IP адрес

# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/alectinib-registry.conf /etc/nginx/sites-enabled/

# Удаление дефолтного сайта (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверка конфигурации
sudo nginx -t

# Перезапуск Nginx
sudo systemctl restart nginx
```

### Шаг 7: Настройка Firewall (опционально)

```bash
# Разрешение HTTP/HTTPS трафика
sudo ufw allow 'Nginx Full'
sudo ufw allow OpenSSH
sudo ufw enable
```

### Шаг 8: Установка SSL сертификата (рекомендуется)

```bash
# Установка Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получение сертификата
sudo certbot --nginx -d your-domain.com

# Автообновление сертификата настроится автоматически
```

### Шаг 9: Финальная проверка

```bash
# Проверка работы backend
curl http://localhost:5000/api/health

# Проверка работы через Nginx
curl http://your-domain.com/api/health

# Просмотр логов backend
sudo journalctl -u alectinib-registry -f

# Просмотр логов Nginx
sudo tail -f /var/log/nginx/alectinib_access.log
sudo tail -f /var/log/nginx/alectinib_error.log
```

## 🔐 Первый вход

### Данные администратора по умолчанию:

- **Логин**: `admin`
- **Пароль**: `Dlyazapolneniya8!`
- **Учреждение**: НМИЦ Онкологии им. Н.Н. Блохина

**⚠️ ВАЖНО: Измените пароль администратора после первого входа!**

### Создание новых пользователей

1. Войдите как администратор
2. Перейдите в раздел "Пользователи" (будет добавлен в будущих версиях) или используйте API:

```bash
curl -X POST http://your-domain.com/api/users \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "new_user",
    "password": "secure_password",
    "role": "user",
    "institution_id": 1
  }'
```

## 🔧 Обслуживание

### Обновление приложения

```bash
cd /var/www/alectinib_registry

# Backup базы данных
sudo cp data/alectinib_registry.db data/alectinib_registry.db.backup

# Получение обновлений (из git)
git pull

# Обновление backend
cd backend
source ../venv/bin/activate
pip install -r requirements.txt

# Обновление frontend
cd ../frontend
npm install
npm run build

# Перезапуск сервиса
sudo systemctl restart alectinib-registry
sudo systemctl reload nginx
```

### Резервное копирование

```bash
# Создание backup директории
sudo mkdir -p /var/backups/alectinib_registry

# Backup базы данных
sudo cp /var/www/alectinib_registry/data/alectinib_registry.db \
       /var/backups/alectinib_registry/alectinib_registry_$(date +%Y%m%d_%H%M%S).db

# Автоматический backup (через crontab)
sudo crontab -e

# Добавьте строку для ежедневного backup в 3:00
# 0 3 * * * cp /var/www/alectinib_registry/data/alectinib_registry.db /var/backups/alectinib_registry/alectinib_registry_$(date +\%Y\%m\%d).db
```

### Просмотр логов

```bash
# Backend логи
sudo journalctl -u alectinib-registry -n 100 --no-pager

# Nginx логи
sudo tail -n 100 /var/log/nginx/alectinib_access.log
sudo tail -n 100 /var/log/nginx/alectinib_error.log

# Мониторинг в реальном времени
sudo journalctl -u alectinib-registry -f
```

### Перезапуск сервисов

```bash
# Перезапуск backend
sudo systemctl restart alectinib-registry

# Перезапуск Nginx
sudo systemctl restart nginx

# Проверка статуса
sudo systemctl status alectinib-registry
sudo systemctl status nginx
```

### Очистка старых данных

```bash
# Очистка audit логов старше 90 дней (через SQL)
sqlite3 /var/www/alectinib_registry/data/alectinib_registry.db \
  "DELETE FROM audit_logs WHERE timestamp < date('now', '-90 days');"

# Vacuum для уменьшения размера БД
sqlite3 /var/www/alectinib_registry/data/alectinib_registry.db "VACUUM;"
```

## 📊 Структура базы данных

### Таблицы:

- **users** - Пользователи системы
- **institutions** - Учреждения
- **patients** - Пациенты
- **clinical_records** - Клинические записи (все поля из документа)
- **dictionaries** - Справочники (comorbidities, alk_methods, и т.д.)
- **audit_logs** - Журнал аудита

## 🐛 Решение проблем

### Backend не запускается

```bash
# Проверка логов
sudo journalctl -u alectinib-registry -n 50

# Проверка порта
sudo netstat -tulpn | grep 5000

# Ручной запуск для отладки
cd /var/www/alectinib_registry/backend
source ../venv/bin/activate
python -m uvicorn main:app --host 0.0.0.0 --port 5000
```

### Frontend показывает ошибки API

1. Проверьте, что backend запущен: `curl http://localhost:5000/api/health`
2. Проверьте Nginx конфигурацию: `sudo nginx -t`
3. Просмотрите Nginx логи ошибок

### База данных заблокирована

```bash
# Остановка всех процессов, использующих БД
sudo systemctl stop alectinib-registry

# Проверка блокировок
lsof /var/www/alectinib_registry/data/alectinib_registry.db

# Запуск снова
sudo systemctl start alectinib-registry
```

## 📝 API Документация

После запуска backend, документация API доступна по адресу:

- Swagger UI: http://your-domain.com:5000/docs
- ReDoc: http://your-domain.com:5000/redoc

## 🤝 Поддержка

При возникновении вопросов или проблем:

1. Проверьте логи сервисов
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность настройки конфигурационных файлов

## 📄 Лицензия

Проект разработан для внутреннего использования.

---

**Версия:** 1.0.0  
**Дата:** Ноябрь 2025
